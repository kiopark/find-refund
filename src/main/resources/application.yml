spring:
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  profiles:
    include: jwt

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create